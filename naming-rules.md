# 文档命名规则

本文档规定了整个项目的文档结构和命名规则，**所有版块都必须遵循**这些通用规则。

## 规则概览

| 位置 | 规则 | 示例 |
|------|------|------|
| **Markdown 文件标题（H1-H6）** | 无编号 | `# JavaScript 核心基础` |
| **左侧导航栏 - 父级分组** | 无编号 | `基础入门` |
| **左侧导航栏 - 子级项目** | 使用"第X章：" | `第1章：AI辅助前端开发` |
| **顶部导航栏** | 与左侧父级一致 | `基础入门` |

## 详细规则

### 1. Markdown 文件内容

所有 Markdown 文件的标题（H1-H6）**禁止使用任何形式的编号**，包括：

- ❌ 数字点分格式：`## 1.1 标题`、`### 1.1.1 标题`
- ❌ 章节编号格式：`## 第1章 标题`、`### 第2章 标题`
- ❌ H1 中的编号：`# 1.2 标题`、`# 第1章：标题`

#### 规则
```markdown
# H1 标题 - 无编号
## H2 标题 - 无编号
### H3 标题 - 无编号
#### H4 标题 - 无编号
##### H5 标题 - 无编号
###### H6 标题 - 无编号
```

#### 正确示例
```markdown
# JavaScript 核心基础

## 什么是数组？

### 数组方法

#### 遍历方法

##### forEach() 方法
```

#### 错误示例
```markdown
# 1.2 JavaScript 核心基础        ❌

## 1.1 什么是数组？              ❌

### 1.1.1 数组方法              ❌

#### 1.1.1.1 遍历方法           ❌
```

### 2. 左侧导航栏配置（sidebar.ts）

#### 父级分组
**规则**：父级分组的 `text` 字段**不使用编号**

```typescript
{
  text: '基础入门',  // ✅ 正确 - 无编号
  // text: '第1章：基础入门',  // ❌ 错误
  items: [
    { text: '第1章：AI辅助前端开发', link: '/guide/chapter-00' },
    { text: '第2章：JavaScript核心基础', link: '/guide/chapter-01' },
  ]
}
```

#### 子级项目
**规则**：子级项目的 `text` 字段**使用"第X章："前缀**

```typescript
{
  text: '基础入门',
  items: [
    { text: '第1章：AI辅助前端开发', link: '/guide/chapter-00' },      // ✅ 正确
    { text: '第2章：JavaScript核心基础', link: '/guide/chapter-01' },  // ✅ 正确
    // { text: 'AI辅助前端开发', link: '/guide/chapter-00' },        // ❌ 错误 - 缺少编号
  ]
}
```

#### 高级补充文档
**规则**：高级补充章节应作为对应基础章节的子节点，使用 `⭐ 高级特性` 格式

```typescript
// ✅ 正确 - 高级章节作为子节点
{
  text: '第15章：组合式API深入',
  link: '/guide/chapter-14',
  items: [
    { text: 'ref 和 reactive', link: '/guide/chapter-14#ref-和-reactive' },
    { text: 'computed 和 watch', link: '/guide/chapter-14#computed-和-watch' },
    { text: '组合式函数', link: '/guide/chapter-14#组合式函数composables' },
    { text: '⭐ 高级特性', link: '/guide/chapter-14-advanced' }  // ✅ 作为子节点
  ]
}

// ❌ 错误 - 高级章节作为独立章节
{
  text: '组件开发',
  items: [
    { text: '第15章：组合式API深入', link: '/guide/chapter-14' },
    { text: '⭐ 第15章（高级）：组合式API特性', link: '/guide/chapter-14-advanced' },  // ❌ 不应独立
  ]
}
```

**说明**：
- 高级补充文档文件名格式：`chapter-XX-advanced.md`
- 在侧边栏中，高级章节应挂在对应基础章节的 `items` 下
- 子节点显示名称统一使用 `⭐ 高级特性`，不需要重复章节编号和标题

#### 嵌套章节配置（带锚点链接）

当一个章节内容较多时，可以添加子节点展示章节内的主要小节（锚点链接）：

```typescript
{
  text: '第17章：Vue Router 路由完全指南',
  link: '/guide/chapter-16',
  items: [
    { text: '安装和配置', link: '/guide/chapter-16#安装和配置' },
    { text: '路由使用', link: '/guide/chapter-16#路由使用' },
    { text: '编程式导航', link: '/guide/chapter-16#编程式导航' },
    { text: '路由守卫与权限控制', link: '/guide/chapter-16#路由守卫与权限控制' },
    { text: '⭐ 高级特性', link: '/guide/chapter-16-advanced' }  // 高级补充
  ]
}
```

**优点**：
- 便于快速导航到章节内的具体内容
- 高级补充内容作为子节点，结构清晰
- 提升用户体验，减少滚动查找

#### 侧边栏锚点配置规则

**重要规则**：sidebar.ts 中配置的锚点链接必须在对应的 Markdown 文件中存在显式定义。

**问题**：如果 sidebar.ts 中配置了锚点，但 Markdown 文件中没有定义，点击侧边栏子节点将无法跳转到对应位置。

**解决方案**：在 Markdown 文件中使用显式锚点语法

**Markdown 文件中定义锚点**：
```markdown
## 安装和配置 {#安装和配置}

内容...

## 路由使用 {#路由使用}

内容...
```

**sidebar.ts 中引用锚点**：
```typescript
{
  text: '第16章：Vue Router 路由完全指南',
  link: '/guide/chapter-16',
  items: [
    { text: '安装和配置', link: '/guide/chapter-16#安装和配置' },  // ✅ 对应 Markdown 中的 {#安装和配置}
    { text: '路由使用', link: '/guide/chapter-16#路由使用' },     // ✅ 对应 Markdown 中的 {#路由使用}
  ]
}
```

**锚点命名规范**：
- ✅ 使用中文：`{#安装和配置}`
- ✅ 使用中文加横线：`{#vueuse简介与安装}`
- ✅ 使用简短英文：`{#核心原则}`
- ❌ 避免特殊字符：`{#技巧25-prompt-chaining提示词链}`（太复杂，难以维护）

**检查和清理工具**：

项目提供了自动化工具来检查和清理无效的锚点配置：

```bash
# 一键检查和修复（包含锚点检查和清理）
./check-and-fix-all.sh

# 或单独运行锚点检查
bash .scripts/check-anchors.sh

# 或单独清理无效锚点
bash .scripts/clean-anchors.sh
```

**工具功能**：
- ✅ 自动检测所有模块的锚点配置
- ✅ 验证锚点是否在 Markdown 文件中存在
- ✅ 自动清理 sidebar.ts 中无效的锚点链接
- ✅ 通用化设计，自动识别所有模块

**修复策略**：
当发现无效锚点时，工具会**从 sidebar.ts 中删除**无效的锚点配置，而不是在 Markdown 文件中添加不存在的标题。这样确保：
- sidebar.ts 配置与实际内容一致
- 用户不会点击到无效的链接
- 保持文档的原始结构

### 3. 学习路径图配置（index.md）

**规则**：学习路径图必须与侧边栏配置保持完全一致

#### 章节编号一致性

学习路径图中的章节编号必须与侧边栏配置一致：

```markdown
📖 基础入门（第1-9章）[⏱️ 17小时 | 🔰 入门]
🧩 组件开发（第10-16章）[⏱️ 20小时 | ⭐ 中级]
🏢 企业级开发（第17-25章）[⏱️ 25小时 | ⭐⭐ 进阶]
```

**对应关系**：
- 侧边栏 `text: '第1章：AI辅助前端开发'` → 学习路径图 `第1章：AI辅助前端开发`
- 侧边栏 `text: '第10章：计算属性与侦听器'` → 学习路径图 `第10章：计算属性与侦听器`

#### 高级补充章节标注

学习路径图中需标注高级补充内容：

```markdown
🧩 组件开发（第10-16章）
   ├─ 组合式API深入
   │  └─ ⭐ 高级特性
```

**规则**：
- 在有高级补充的章节下添加 `└─ ⭐ 高级特性` 标识
- 与侧边栏中的 `{ text: '⭐ 高级特性', link: '...' }` 对应

#### 模块划分对应

学习路径图的模块划分必须与侧边栏的父级分组一致：

```typescript
// 侧边栏配置
{
  text: '基础入门',      // → 学习路径图：📖 基础入门
  items: [
    { text: '第1章：...' },
    // ...
  ]
}
```

#### 完整示例

```markdown
📖 基筑入门（第1-9章）[⏱️ 17小时 | 🔰 入门]
   ├─ 第1章：AI辅助前端开发
   ├─ 第2章：JavaScript核心基础
   ├─ 第3章：Vue3简介与环境搭建
   ├─ 第4章：ESLint代码检查
   ├─ 第5章：CSS基础语法
   ├─ 第6章：CSS预处理器 - Less
   ├─ 第7章：CSS预处理器 - SCSS
   ├─ 第8章：代码规范
   └─ 第9章：模板语法与数据绑定
                           ↓
🧩 组件开发（第10-16章）[⏱️ 20小时 | ⭐ 中级]
   ├─ 第10章：计算属性与侦听器
   ├─ 第11章：条件渲染与列表渲染
   ├─ 第12章：事件处理与表单绑定
   ├─ 第13章：组件基础与组件名称定义
   ├─ 第14章：组件通信（完整版）
   ├─ 第15章：组合式API深入
   │  └─ ⭐ 高级特性        ← 高级补充标注
   └─ 第16章：生命周期与钩子函数
```

**同步更新清单**：

修改侧边栏配置后，必须同步更新学习路径图：
- [ ] 章节编号已更新
- [ ] 模块划分已调整
- [ ] 高级补充章节已标注
- [ ] 章节顺序已重新排列

### 4. 顶部导航栏配置（nav.ts）

**规则**：顶部导航的分组名称与侧边栏父级保持一致（无编号）

```typescript
{
  text: "前端开发",
  items: [
    { text: "学习路线", link: "/guide/" },      // ✅ 正确 - 无编号
    { text: "基础入门", link: "/guide/chapter-00" },  // ✅ 正确
    // { text: "第1章：基础入门", link: "/guide/chapter-00" },  // ❌ 错误
  ],
}
```

**一致性要求**：

顶部导航栏的子菜单项必须与侧边栏的父级分组名称完全一致：

| 顶部导航栏 | 侧边栏父级 | 状态 |
|-----------|-----------|------|
| 基础入门 | `text: '基础入门'` | ✅ 一致 |
| 组件开发 | `text: '组件开发'` | ✅ 一致 |
| 企业级开发 | `text: '企业级开发'` | ✅ 一致 |
| 进阶部分 | `text: '进阶部分'` | ✅ 一致 |
| 高级拓展 | `text: '高级拓展'` | ✅ 一致 |

**检查工具**：

运行一致性检查脚本验证 nav.ts 和 sidebar.ts 的对应关系：

```bash
# 单独运行导航栏一致性检查
bash .scripts/check-nav-sidebar-consistency.sh

# 或包含在一键检查中
./check-and-fix-all.sh
```

**功能**：
- ✅ 自动提取 nav.ts 和 sidebar.ts 的分组信息
- ✅ 验证顶部导航栏的子项是否都在侧边栏中存在对应的父级分组
- ✅ 生成详细的对比报告
- ✅ 通用化设计，自动检测所有模块

---

## 一键修复脚本

**重要提示**：由于 Windows bat 文件编码限制，对中文"第X章"的检测可能不准确。**推荐使用 Bash 脚本**（Git Bash 可用），它们能正确处理中文。

### 检查脚本

#### Bash / Git Bash（推荐）

保存为 `check-naming-rules.sh`：

```bash
#!/bin/bash
# Check Naming Rules

echo "=== Check Naming Rules ==="
echo ""

echo "[1] H2 Numbered (1.1):"
grep -rn '^##\s\+[0-9]\+\.[0-9]\+\s\+' docs/ 2>/dev/null | wc -l

echo "[2] H2 Chapter (Chapter X):"
grep -rn '^##\s\+第[0-9]\+\s*章' docs/ 2>/dev/null | wc -l

echo "[3] H3 Numbered (1.1.1):"
grep -rn '^###\s\+[0-9]\+\.[0-9]\+\.[0-9]\+\s\+' docs/ 2>/dev/null | wc -l

echo "[4] H3 Chapter (Chapter X):"
grep -rn '^###\s\+第[0-9]\+\s*章' docs/ 2>/dev/null | wc -l

echo "[5] H4 Numbered (1.1.1.1):"
grep -rn '^####\s\+[0-9]\+\.[0-9]\+\.[0-9]\+\.[0-9]\+\s\+' docs/ 2>/dev/null | wc -l

echo "[6] H4 Chapter (Chapter X):"
grep -rn '^####\s\+第[0-9]\+\s*章' docs/ 2>/dev/null | wc -l

echo "[7] H1 Numbered:"
grep -rn '^#\s\+[0-9]\+\.[0-9]\+\s\+' docs/ 2>/dev/null | wc -l

echo "[8] H1 Chapter (Chapter X):"
grep -rn '^#\s\+第[0-9]\+\s*章' docs/ 2>/dev/null | wc -l
```
```

#### 学习路径图检查脚本

**重要说明**：本项目使用**通用化自动检测脚本**，不针对特定模块。脚本会自动发现所有模块并检查学习路径图的一致性。

检查学习路径图 (`docs/*/index.md`) 与侧边栏配置 (`docs/.vitepress/sidebar.ts`) 的一致性。

**使用方法**：

```bash
# 运行通用化的学习路径图检查脚本
./fix-learning-path.sh    # 或 fix-learning-path.bat (Windows)
```

**脚本特点**：
- ✅ **自动发现模块** - 扫描 `docs/` 目录，自动识别所有模块（不硬编码模块名）
- ✅ **智能检测** - 从 sidebar.ts 提取章节信息，与 index.md 学习路径图对比
- ✅ **未来兼容** - 新增模块后无需修改脚本，自动检测

**实现逻辑**：
1. 自动扫描 `docs/` 目录下所有包含 `index.md` 的子目录
2. 从 `sidebar.ts` 中提取每个模块的所有章节编号
3. 检查 `index.md` 中的学习路径图是否覆盖了所有章节
4. 支持多分组学习路径图（如：基础入门、进阶、高级等）

#### PowerShell (Windows 推荐)

保存为 `check-naming-rules.ps1`：

```powershell
# 检查文档命名规范

Write-Host "=== 检查命名规范 ===" -ForegroundColor Cyan
Write-Host ""

# 检查 H2 标题编号
Write-Host "H2 标题编号违规:" -ForegroundColor Cyan
$h2Violations = Select-String -Path "docs\**\*.md" -Pattern "^##\s+\d+\.\d+\s+"
Write-Host "  数量: $($h2Violations.Count)" -ForegroundColor $(if ($h2Violations.Count -eq 0) { "Green" } else { "Red" })

# 检查 H3 标题编号
Write-Host "H3 标题编号违规:" -ForegroundColor Cyan
$h3Violations = Select-String -Path "docs\**\*.md" -Pattern "^###\s+\d+\.\d+\.\d+\s+"
Write-Host "  数量: $($h3Violations.Count)" -ForegroundColor $(if ($h3Violations.Count -eq 0) { "Green" } else { "Red" })

# 检查 H4 标题编号
Write-Host "H4 标题编号违规:" -ForegroundColor Cyan
$h4Violations = Select-String -Path "docs\**\*.md" -Pattern "^####\s+\d+\.\d+\.\d+\.\d+\s+"
Write-Host "  数量: $($h4Violations.Count)" -ForegroundColor $(if ($h4Violations.Count -eq 0) { "Green" } else { "Red" })

# 检查 H1 标题违规
Write-Host "H1 标题违规:" -ForegroundColor Cyan
$h1Violations = Select-String -Path "docs\**\*.md" -Pattern "^#\s+\d+\.\d+\s+|^#\s+第\d+章："
Write-Host "  数量: $($h1Violations.Count)" -ForegroundColor $(if ($h1Violations.Count -eq 0) { "Green" } else { "Red" })

$total = $h2Violations.Count + $h3Violations.Count + $h4Violations.Count + $h1Violations.Count
Write-Host ""
Write-Host "=== 总计 ===" -ForegroundColor Cyan
Write-Host "总违规数量: $total" -ForegroundColor $(if ($total -eq 0) { "Green" } else { "Red" })

if ($total -eq 0) {
    Write-Host "✅ 所有文档符合命名规范！" -ForegroundColor Green
    exit 0
} else {
    Write-Host "❌ 发现 $total 处违规，请运行 fix-naming-rules.ps1 修复" -ForegroundColor Red

    # 显示违规详情
    if ($h2Violations.Count -gt 0) {
        Write-Host ""
        Write-Host "H2 违规详情：" -ForegroundColor Yellow
        $h2Violations | Select-Object -First 10 | ForEach-Object {
            Write-Host "  $($_.Path):$($_.LineNumber): $($_.Line.Trim())" -ForegroundColor Red
        }
        if ($h2Violations.Count -gt 10) {
            Write-Host "  ... 还有 $($h2Violations.Count - 10) 个" -ForegroundColor Gray
        }
    }
    exit 1
}
```

#### Batch (Windows CMD)

保存为 `check-naming-rules.bat`：

```batch
@echo off
echo === 检查命名规范 ===
echo.

echo H2 标题编号违规:
findstr /R /S "^##[0-9][0-9]*.[0-9][0-9]* " docs\**\*.md 2>nul | find /c /v ""

echo.
echo H3 标题编号违规:
findstr /R /S "^###[0-9][0-9]*.[0-9][0-9]*.[0-9][0-9]* " docs\**\*.md 2>nul | find /c /v ""

echo.
echo H4 标题编号违规:
findstr /R /S "^####[0-9][0-9]*.[0-9][0-9]*.[0-9][0-9]*.[0-9][0-9]* " docs\**\*.md 2>nul | find /c /v ""

echo.
echo H1 标题违规:
findstr /R /S "^#[0-9][0-9]*.[0-9][0-9]* " docs\**\*.md 2>nul | find /c /v ""

echo.
echo 如果发现违规，请运行相应的修复脚本
pause
```

---

### 修复脚本

#### Bash / Git Bash（推荐）

保存为 `fix-naming-rules.sh`：

```bash
#!/bin/bash
# Fix Naming Violations

echo "=== Fixing Naming Violations ==="
echo ""

# Count before
h2_num=$(grep -rn '^##\s\+[0-9]\+\.[0-9]\+\s\+' docs/ 2>/dev/null | wc -l)
h2_chap=$(grep -rn '^##\s\+第[0-9]\+\s*章' docs/ 2>/dev/null | wc -l)
h3_num=$(grep -rn '^###\s\+[0-9]\+\.[0-9]\+\.[0-9]\+\s\+' docs/ 2>/dev/null | wc -l)
h3_chap=$(grep -rn '^###\s\+第[0-9]\+\s*章' docs/ 2>/dev/null | wc -l)
h4_num=$(grep -rn '^####\s\+[0-9]\+\.[0-9]\+\.[0-9]\+\.[0-9]\+\s\+' docs/ 2>/dev/null | wc -l)
h4_chap=$(grep -rn '^####\s\+第[0-9]\+\s*章' docs/ 2>/dev/null | wc -l)
h1_num=$(grep -rn '^#\s\+[0-9]\+\.[0-9]\+\s\+' docs/ 2>/dev/null | wc -l)
h1_chap=$(grep -rn '^#\s\+第[0-9]\+\s*章' docs/ 2>/dev/null | wc -l)

echo "Before:"
echo "  H2 Numbered: $h2_num, Chapter: $h2_chap"
echo "  H3 Numbered: $h3_num, Chapter: $h3_chap"
echo "  H4 Numbered: $h4_num, Chapter: $h4_chap"
echo "  H1 Numbered: $h1_num, Chapter: $h1_chap"
echo ""

# Fix
find docs/ -name "*.md" -type f -exec sed -i 's/^##\s\+[0-9]\+\.[0-9]\+\s\+/## /g' {} \;
find docs/ -name "*.md" -type f -exec sed -i 's/^##\s\+第[0-9]\+\s*章\s*/## /g' {} \;
find docs/ -name "*.md" -type f -exec sed -i 's/^###\s\+[0-9]\+\.[0-9]\+\.[0-9]\+\s\+/### /g' {} \;
find docs/ -name "*.md" -type f -exec sed -i 's/^###\s\+第[0-9]\+\s*章\s*/### /g' {} \;
find docs/ -name "*.md" -type f -exec sed -i 's/^####\s\+[0-9]\+\.[0-9]\+\.[0-9]\+\.[0-9]\+\s\+/#### /g' {} \;
find docs/ -name "*.md" -type f -exec sed -i 's/^####\s\+第[0-9]\+\s*章\s*/#### /g' {} \;
find docs/ -name "*.md" -type f -exec sed -i 's/^#\s\+[0-9]\+\.[0-9]\+\s\+/# /g' {} \;
find docs/ -name "*.md" -type f -exec sed -i 's/^#\s\+第[0-9]\+\s*章\s*/# /g' {} \;

echo "Done! Run check-naming-rules.sh to verify."
```

#### PowerShell (Windows 推荐)

保存为 `fix-naming-rules.ps1`：

```powershell
# 修复文档命名规范

Write-Host "=== 开始修复命名违规 ===" -ForegroundColor Cyan
Write-Host ""

# 统计修复前
Write-Host "修复前统计：" -ForegroundColor Yellow
. "$PSScriptRoot\check-naming-rules.ps1"
Write-Host ""

# 获取所有 markdown 文件
$files = Get-ChildItem -Path "docs" -Filter "*.md" -Recurse -File

$totalFiles = $files.Count
$processedFiles = 0
$h2Fixed = 0
$h3Fixed = 0
$h4Fixed = 0

foreach ($file in $files) {
    $content = Get-Content $file.FullName -Raw -Encoding UTF8
    $originalContent = $content

    # 修复 H2 标题
    $h2Matches = [regex]::Matches($content, '^##\s+\d+\.\d+\s+', [System.Text.RegularExpressions.RegexOptions]::Multiline)
    if ($h2Matches.Count -gt 0) {
        $content = [regex]::Replace($content, '^##\s+\d+\.\d+\s+', '## ', [System.Text.RegularExpressions.RegexOptions]::Multiline)
        $h2Fixed += $h2Matches.Count
    }

    # 修复 H3 标题
    $h3Matches = [regex]::Matches($content, '^###\s+\d+\.\d+\.\d+\s+', [System.Text.RegularExpressions.RegexOptions]::Multiline)
    if ($h3Matches.Count -gt 0) {
        $content = [regex]::Replace($content, '^###\s+\d+\.\d+\.\d+\s+', '### ', [System.Text.RegularExpressions.RegexOptions]::Multiline)
        $h3Fixed += $h3Matches.Count
    }

    # 修复 H4 标题
    $h4Matches = [regex]::Matches($content, '^####\s+\d+\.\d+\.\d+\.\d+\s+', [System.Text.RegularExpressions.RegexOptions]::Multiline)
    if ($h4Matches.Count -gt 0) {
        $content = [regex]::Replace($content, '^####\s+\d+\.\d+\.\d+\.\d+\s+', '#### ', [System.Text.RegularExpressions.RegexOptions]::Multiline)
        $h4Fixed += $h4Matches.Count
    }

    # 只有内容改变时才写入文件
    if ($content -ne $originalContent) {
        [System.IO.File]::WriteAllText($file.FullName, $content, [System.Text.UTF8Encoding]::new($false))
    }

    $processedFiles++

    # 显示进度
    if ($processedFiles % 10 -eq 0) {
        Write-Host "进度: $processedFiles / $totalFiles" -ForegroundColor Gray
    }
}

Write-Host ""
Write-Host "=== 修复完成 ===" -ForegroundColor Green
Write-Host "  - 处理文件数: $totalFiles" -ForegroundColor Cyan
Write-Host "  - 修复 H2 标题: $h2Fixed 个" -ForegroundColor Cyan
Write-Host "  - 修复 H3 标题: $h3Fixed 个" -ForegroundColor Cyan
Write-Host "  - 修复 H4 标题: $h4Fixed 个" -ForegroundColor Cyan
Write-Host ""

# 统计修复后
Write-Host "修复后统计：" -ForegroundColor Yellow
. "$PSScriptRoot\check-naming-rules.ps1"
```

---

## 使用方法

### 一键检测与修复（推荐）

**综合脚本**：自动完成所有检测和修复

```bash
# Git Bash / Linux / macOS
./check-and-fix-all.sh

# Windows (推荐使用 Git Bash)
bash check-and-fix-all.sh
```

**功能**：
- ✅ 自动检测 Markdown 标题编号违规
- ✅ 检查侧边栏锚点配置有效性
- ✅ 自动清理 sidebar.ts 中无效的锚点链接
- ✅ 检查学习路径图一致性（自动检测所有模块）
- ✅ 验证顶部导航栏与侧边栏的对应关系
- ✅ 生成完整检查报告

### 侧边栏锚点检查与清理

**锚点检查脚本**：自动检查 sidebar.ts 中配置的所有锚点是否在 Markdown 文件中存在

```bash
# Git Bash / Linux / macOS
bash .scripts/check-anchors.sh

# Windows
bash .scripts/check-anchors.sh
```

**功能**：
- 🔍 自动提取 sidebar.ts 中所有锚点配置
- ✅ 验证锚点是否在对应 Markdown 文件中存在
- 📊 生成详细的检查报告，列出所有无效锚点
- 🎯 通用检测，适用于所有模块

**锚点清理脚本**：自动从 sidebar.ts 中删除无效的锚点配置

```bash
# Git Bash / Linux / macOS
bash .scripts/clean-anchors.sh

# Windows
bash .scripts/clean-anchors.sh
```

**功能**：
- 🗑️ 自动删除 sidebar.ts 中无效的锚点链接
- ✅ 保持 sidebar 配置与 Markdown 文件内容一致
- 📝 生成清理报告，说明删除了哪些锚点
- 🎯 确保用户点击侧边栏时能正确跳转

**使用场景**：
- 添加或删除 sidebar.ts 子节点锚点后
- 修改 Markdown 文件标题后
- 发现侧边栏点击无法跳转时

### 学习路径图自动检测与修复

**学习路径图检查脚本**：自动从 sidebar.ts 提取章节信息，检查所有模块的学习路径图

```bash
# Git Bash / Linux / macOS
./fix-learning-path.sh

# Windows
fix-learning-path.bat
```

**功能**：
- 📊 自动解析 `sidebar.ts`，提取所有模块的章节定义
- 🔍 检查每个模块的 `index.md` 学习路径图
- ✅ 支持多分组的学习路径图（如：第1-9章、第10-16章等）
- 📝 生成详细的检查报告和修复建议
- 🎯 通用检测，自动识别所有模块（guide、ai、git 以及未来新增模块）

**检测逻辑**：
- 从 sidebar.ts 提取每个模块的完整章节范围
- 检查 index.md 中的所有分组范围并集是否覆盖 sidebar 中的所有章节
- 支持多个分组的场景（如基础入门、进阶、高级等）

**输出示例**：
```
=== 自动修复学习路径图 ===

[步骤 1/3] 解析 sidebar.ts 中的章节信息...
  检查 guide 模块...
    发现 48 个章节: 第1-48章
  检查 ai 模块...
    发现 9 个章节: 第1-9章
  检查 git 模块...
    发现 5 个章节: 第1-5章

[步骤 2/3] 检查并更新 index.md 文件...
[guide]
  期望范围: 第1-48章
  ✅ 学习路径图已是最新

========================================
          学习路径图检查报告
========================================
✅ [guide] 学习路径图一致 (第1-48章)
✅ [ai] 学习路径图一致 (第1-9章)
✅ [git] 学习路径图一致 (第1-5章)
```

### 分步执行

#### Git Bash / Linux / macOS

**注意**：bat 版本由于编码限制，对中文"第X章"的检测可能不准确。推荐使用 Git Bash 运行 `.sh` 脚本。

```batch
REM 检查命名规范
check-naming-rules.bat

REM 如果有违规，运行修复
fix-naming-rules.bat
```

```bash
# 1. 赋予执行权限
chmod +x check-naming-rules.sh fix-naming-rules.sh

# 2. 检查命名规范
./check-naming-rules.sh

# 3. 如果有违规，运行修复
./fix-naming-rules.sh

# 4. 再次检查确认
./check-naming-rules.sh
```

### Windows PowerShell (推荐)

```powershell
# 1. 检查命名规范
powershell -ExecutionPolicy Bypass -File check-naming-rules.ps1

# 2. 如果有违规，运行修复
powershell -ExecutionPolicy Bypass -File fix-naming-rules.ps1

# 3. 再次检查确认
powershell -ExecutionPolicy Bypass -File check-naming-rules.ps1
```

### Windows CMD

```batch
REM 1. 检查命名规范
check-naming-rules.bat

REM 2. 如果有违规，使用 PowerShell 或 Git Bash 运行修复脚本
REM （Windows CMD 不支持正则替换）
```

---

## 常见问题

### Q1: PowerShell 执行策略限制

**错误**：无法加载文件，因为在此系统上禁止运行脚本

**解决**：
```powershell
# 临时允许（推荐）
powershell -ExecutionPolicy Bypass -File check-naming-rules.ps1

# 或者永久设置
Set-ExecutionPolicy RemoteSigned -Scope CurrentUser
```

### Q2: 修复后仍有违规

**原因**：可能是缓存问题

**解决**：
```bash
# Linux/macOS
rm -rf docs/.vitepress/cache

# Windows PowerShell
Remove-Item -Recurse -Force docs\.vitepress\cache
```

### Q3: 推荐使用哪个脚本

**推荐**：PowerShell 脚本（`.ps1`）

优点：
- Windows 原生支持，无需额外安装
- 支持 .NET 正则表达式，功能强大
- 跨平台兼容（Linux/macOS 也可安装 PowerShell）
- 更好的错误处理和输出格式

---

## 检查清单

提交代码前，请确保：

- [ ] **Markdown 文件 H1-H6 标题无编号**
- [ ] **左侧导航栏父级分组无编号**
- [ ] **左侧导航栏子级项目有"第X章："前缀**
- [ ] **高级补充文档作为基础章节的子节点，使用 `⭐ 高级特性` 格式**
- [ ] **顶部导航栏与侧边栏父级一致**
- [ ] **学习路径图与侧边栏配置一致**
- [ ] **已运行检查脚本，无违规**
  - **推荐**：`./check-and-fix-all.sh` - 一键完成所有检测和修复
  - 或分别运行：
    - `bash .scripts/check-naming-rules.sh` - 检查 Markdown 标题规范
    - `bash .scripts/check-anchors.sh` - 检查侧边栏锚点有效性
    - `bash .scripts/fix-learning-path.sh` - 检查学习路径图一致性

### 侧边栏锚点检查

- [ ] **sidebar.ts 锚点配置有效**
  - sidebar.ts 中配置的锚点必须在对应 Markdown 文件中存在 ✅
  - 使用显式锚点语法：`## 标题 {#锚点}` ✅
  - 运行检查脚本：`bash .scripts/check-anchors.sh`

- [ ] **无效锚点已清理**
  - 发现无效锚点后运行清理脚本：`bash .scripts/clean-anchors.sh`
  - 确保点击侧边栏子节点能正确跳转到对应位置
  - 避免配置不存在的锚点导致跳转失败

- [ ] **Markdown 文件中定义了显式锚点**
  - 如果 sidebar.ts 中使用了锚点链接，必须在 Markdown 中定义
  - 示例：sidebar 中 `/guide/chapter-16#安装和配置` → Markdown 中 `## 安装和配置 {#安装和配置}` ✅
  - 锚点命名规范：使用中文或简短英文，避免特殊字符

### 侧边栏配置检查

- [ ] **父级分组无编号**
  - 示例：`text: '基础入门'` ✅
  - 错误：`text: '第1章：基础入门'` ❌

- [ ] **子级项目有编号**
  - 示例：`text: '第1章：AI辅助前端开发'` ✅
  - 错误：`text: 'AI辅助前端开发'` ❌

- [ ] **高级补充文档嵌套正确**
  - 示例：作为基础章节的子节点 `items: [..., { text: '⭐ 高级特性', link: '...' }]` ✅
  - 错误：作为独立章节 `items: [..., { text: '⭐ 第X章（高级）：...' }]` ❌

### 顶部导航栏检查（nav.ts）

- [ ] **顶部导航栏与侧边栏完全对应**
  - 运行一致性检查：`bash .scripts/check-nav-sidebar-consistency.sh`
  - 确保 nav.ts 的子菜单项在 sidebar.ts 中都有对应的父级分组 ✅
  - 例如：nav.ts 中 `{ text: "基础入门", link: "/guide/chapter-00" }` → sidebar.ts 中 `text: '基础入门'` ✅

- [ ] **导航栏分组无编号**
  - 示例：nav.ts 中 `{ text: "基础入门", ... }` ✅
  - 错误：nav.ts 中 `{ text: "第1章：基础入门", ... }` ❌

- [ ] **分组名称完全一致**
  - nav.ts 中的 `text` 必须与 sidebar.ts 中的父级 `text` 完全相同
  - 包括中文字符、空格、标点符号
  - 示例：都是 `基础入门` ✅ | 一个是 `基础入门` 一个是 `基础入门 ` ❌

### 学习路径图检查（index.md）

- [ ] **学习路径图与侧边栏配置一致**
  - 章节编号范围匹配：`基础入门（第1-9章）` 与侧边栏章节对应 ✅
  - 模块划分对应：侧边栏父级分组 → 学习路径图模块 ✅
  - 高级补充已标注：在对应章节下添加 `└─ ⭐ 高级特性` ✅

- [ ] **章节编号准确性**
  - 检查侧边栏章节编号与学习路径图是否一致
  - 示例：`第15章：组合式API深入` → `第15章：组合式API深入` ✅

- [ ] **同步更新检查**
  - 修改侧边栏后，同步更新学习路径图
  - 添加/删除章节后，更新路径图的章节范围
  - 添加高级补充文档后，在学习路径图中标注 ⭐

- [ ] **运行学习路径检查脚本**
  - 前端模块：`check-learning-path.bat` 检查 guide/index.md
  - AI模块：检查 ai/index.md 章节范围
  - Git模块：检查 git/index.md 章节范围
  - 确保所有模块的章节范围与侧边栏一致
